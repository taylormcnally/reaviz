import { Meta, Story } from '@storybook/addon-docs/blocks';
import { Sandbox } from '../tools';

<Meta title="Docs|Chart Types/Scatter Plot" />

# Scatter Plot

---
<br />
<Story id="demos-scatter-plot-linear--simple" />
<br />

A scatter plot is a type of mathematical diagram using Cartesian coordinates to
display values for two variables for a set of data. The data is displayed as a collection
of points, each having the value of one variable determining the position on the horizontal
axis and the value of the other variable determining the position on the vertical axis.

## Quick Start
To create a Bar chart, use import the `ScatterPlot` and give it `data`. The chart
will automatically configure itself with the default options exposed via `props`.

```jsx
import { ScatterPlot } from 'reaviz';

const MyChart = () => (
  <ScatterPlot
    height={300}
    width={300}
    data={[
      { key: new Date('11/29/2019'), data: 13 },
      { key: new Date('11/30/2019'), data: 13 },
      { key: new Date('12/1/2019'), data: 13 },
    ]}
  />
);
```

## API

Below is the list of props the chart accepts.

| Prop  | Description | Type | Required | Default |
| --- | --- | --- | --- | --- |
| `height` | The height of the chart | `number` | `false` | `null` |
| `width` | The width of the chart | `number` | `false` | `null` |
| `margins` | Margins around the chart | `number` | `false` | `20` |
| `data` | The data that is used to build the chart | `ChartDataShape[]` | `true` | `[]` |
| `className` | Additional class names to apply to the container | `string` | `false` | `null`  |
| `style` | Additional styles names to apply to the container | `Object` | `false` | `null`  |
| `series` | The area series configuration | `JSX.Element` | `true` | `[]`  |
| `yAxis` | The y-axis configuration | `JSX.Element` | `true` | `<LinearYAxis />`  |
| `xAxis` | The x-axis configuration | `JSX.Element` | `true` | `<LinearXAxis />`  |
| `gridlines` | The gridline configuration | `JSX.Element` | `false` | `null`  |
| `brush` | The brush configuration | `JSX.Element` | `false` | `null`  |
| `zoomPan` | The zoom/pan configuration | `JSX.Element` | `false` | `null`  |
| `secondaryAxis` | Additional axis configuration | `JSX.Element[]` | `false` | `null`  |

## Demo

<Sandbox
  name="Scatter Chart Simple"
  description="A simple scatter chart example"
  path="docs/sandbox/ScatterSimple.sandbox.js"
/>
